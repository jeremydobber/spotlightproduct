{% extends '@PrestaShop/Admin/layout.html.twig' %}

{% block content %}
	{{ form_start(ProductspotlightForm) }}
	<div class="card">
		<h3 class="card-header">
			<i class="material-icons">settings</i>
			{{ 'Select spotlight product'|trans({}, 'Modules.Productspotlight.Admin') }}
		</h3>
		<div class="card-body">
			<div class="form-wrapper">
				{{ form_widget(ProductspotlightForm) }}
			</div>
			{# <table class="grid-table">
				<thead>
					<tr class="column-headers">
						<th>
							ID
						</th>
						<th>
							Product name
						</th>
					</tr>
				</thead>
				<tbody>
				{% for product in ProductList %}
					<tr>
						<td>
							{{product.id_product}}
						</td>
						<td>
							{{product.name}}
						</td>
					</tr>
				{% endfor %}
				</tbody>
			</table> #}
		</div>
		<div class="card-footer">
			<div class="d-flex justify-content-end">
				<button class="btn btn-primary float-right" id="save-button">
					{{ 'Save'|trans({}, 'Admin.Actions') }}
				</button>
			</div>
		</div>
	</div>
	{{ form_end(ProductspotlightForm) }}
{% endblock %}
